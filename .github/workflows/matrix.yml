name: Maxtrix

on: push

jobs:
  node-version:
    runs-on: ubuntu-latest
    steps:
      - name: Test Node version
        run: node -v
      - uses: actions/setup-node@v1
        with: 
          node-version: '10.x'
      - name: Log new version
        run: node -v