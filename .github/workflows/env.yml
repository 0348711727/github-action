name: GIT ENV

on: [push]

env: 
  WF_ENV: Available to all jobs

jobs: 
  log-env: 
    runs-on: ubuntu-latest
    env: 
      JOB_ENV: Available to all steps
    steps:
      - name: echo ae
        run: | 
          echo "WF_ENV": ${WF_ENV}
          echo "JOB_ENV": ${JOB_ENV}
